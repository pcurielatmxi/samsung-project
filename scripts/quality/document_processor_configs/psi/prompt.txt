Extract STRUCTURED DATA from this PSI Daily Field Report. Be CONCISE - do not quote verbatim text.

OUTPUT REQUIREMENTS:
- Keep response under 1500 tokens
- Summarize, don't quote
- Use short field values

EXTRACT:

1. **report_metadata**:
   - report_number (e.g., "DFR:0306103-100-O1")
   - date_of_issue (YYYY-MM-DD)

2. **inspection_details**:
   - test_date (YYYY-MM-DD)
   - contractor (company name only: "Yates", "SECAI", "Berg", etc.)
   - location (building + level + grid, e.g., "FAB1 L4 D/13-17")

3. **inspection_type**:
   - primary_type (short: "Paint Touch Up", "Framing", "Drywall", etc.)

4. **wir_details** (if present):
   - ir_number
   - discipline ("Arch", "Mech", "Elec", "Struct", "Civil")

5. **inspection_result**:
   - first_inspection: {date, result: "Passed"/"Failed"/"Re-Inspection Required"}
   - second_inspection (if exists): {date, result}

6. **deficiencies** (array, max 5 entries):
   - deficiency_type ("Missing Item", "Improper Installation", "Damaged", "Out of Tolerance", "Incomplete", "Other")
   - corrected_during_inspection (true/false)
   - root_cause_category ("Workmanship", "Material", "Design", "Predecessor", "Unknown")

7. **overall_status**: "Passed" or "Failed" or "Re-Inspection Required"

8. **photos**: {has_photos: true/false, count: N}

RULES:
- Omit empty/missing fields
- No long descriptions - 10 words max per field
- Combine similar deficiencies
